info:
  name: Get All Positions
  type: graphql
  seq: 3

graphql:
  method: POST
  url: "{{graphqlUrl}}"
  body:
    query: |
      query GetPositions {
        getPositions {
          userId
          spiceGradeId
          totalQty
          totalCost
          avgCost
          todayPrice
          realizedPnL
          unrealizedPnL
          updatedAt
        }
      }
    variables: |
      {}
  auth:
    type: bearer
    token: "{{merchantAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get all portfolio positions for authenticated merchant.
