info:
  name: List All Transactions
  type: graphql
  seq: 4

graphql:
  method: POST
  url: "{{graphqlUrl}}"
  body:
    query: |
      query ListTransactions($skip: Int, $take: Int) {
        listTransactions(skip: $skip, take: $take) {
          id
          userId
          spiceGradeId
          type
          quantity
          price
          tradeDate
          createdAt
        }
      }
    variables: |
      {
        "skip": 0,
        "take": 10
      }
  auth:
    type: bearer
    token: "{{merchantAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Paginated trade history across all grades for the authenticated merchant.
  Admin JWT with empty user_id lists all users (admin dashboard use case).

  **gRPC:** MarketService.ListTransactions
