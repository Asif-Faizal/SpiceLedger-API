info:
  name: List Products
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/products/"
  auth:
    type: basic
    username: "{{basicAuthUser}}"
    password: "{{basicAuthPass}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
