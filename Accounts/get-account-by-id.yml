info:
  name: Get Account by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/accounts/{{merchantAccountId}}"
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
