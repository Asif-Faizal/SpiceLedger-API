info:
  name: Get Account Info
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/accounts/info"
  auth:
    type: bearer
    token: "{{merchantAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
