info:
  name: Check Email
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/accounts/check-email?email=merchant@spice.com"
  params:
    - name: email
      value: merchant@spice.com
      type: query
  auth:
    type: basic
    username: "{{basicAuthUser}}"
    password: "{{basicAuthPass}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
