info:
  name: GraphQL Health
  type: http
  seq: 4

http:
  method: GET
  url: "{{graphqlDirectUrl}}/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
