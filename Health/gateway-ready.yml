info:
  name: Gateway Ready
  type: http
  seq: 2

http:
  method: GET
  url: "{{gatewayUrl}}/ready"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
