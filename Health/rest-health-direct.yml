info:
  name: REST Health Direct
  type: http
  seq: 3

http:
  method: GET
  url: "{{restDirectUrl}}/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
