info:
  name: Gateway Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{gatewayUrl}}/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
